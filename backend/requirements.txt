# backend/requirements.txt
# De-Emergent'd. No private indexes, no CDN wheels — everything here is on PyPI.
# Slimmed from 130 packages to what server.py / auth_utils.py / database.py /
# storage_utils.py / models.py actually import (plus transitive pins pip needs).

# --- Web framework ---
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
python-multipart==0.0.9        # required for UploadFile / File()
pydantic==2.9.2
pydantic_core==2.23.4
annotated-types==0.7.0
typing_extensions==4.12.2
anyio==4.6.2
sniffio==1.3.1
h11==0.14.0
idna==3.10
click==8.1.7

# --- Database ---
motor==3.6.0                   # bumped from 3.3.1: 3.3.x predates Python 3.12/3.13
pymongo==4.9.2
dnspython==2.7.0               # needed for mongodb+srv:// connection strings

# --- Auth ---
bcrypt==4.2.0
PyJWT==2.9.0

# --- Config ---
python-dotenv==1.0.1

# --- Object storage (S3 / Cloudflare R2 / Backblaze B2 / MinIO) ---
# Only needed if STORAGE_BACKEND=s3. Safe to delete both lines if you use
# STORAGE_BACKEND=gridfs and want a smaller image.
boto3==1.35.54
botocore==1.35.54
s3transfer==0.10.3
jmespath==1.0.1
python-dateutil==2.9.0.post0
six==1.16.0
urllib3==2.2.3

# --- HTTP ---
requests==2.32.3
certifi==2024.8.30
charset-normalizer==3.4.0

# =====================================================================
# REMOVED — and why
# =====================================================================
# emergentintegrations==0.2.0   Not on PyPI. Emergent's private package.
#                               Not imported anywhere in this codebase.
# litellm @ https://customer-assets.emergentagent.com/...  Emergent CDN wheel.
#                               No LLM calls exist in this repo.
# openai, google-genai, google-generativeai, google-ai-generativelanguage,
# google-api-core, google-api-python-client, google-auth, googleapis-common-protos,
# grpcio, grpcio-status, proto-plus, protobuf, tiktoken, tokenizers,
# huggingface_hub, hf-xet, jiter, distro
#                               LLM SDK cruft Emergent installs by default.
#                               Nothing in this repo imports any of it.
# black, flake8, isort, mypy, pytest, pytest-xdist, pycodestyle, pyflakes,
# mccabe, execnet, iniconfig, pluggy, pathspec, pytokens, mypy_extensions
#                               Dev tooling. Move to requirements-dev.txt if
#                               you want it; it does not belong in a prod build.
# pandas, numpy                 Not imported. ~80MB and a long compile.
# stripe                        Not imported. Add back when you wire payments.
# passlib, python-jose, ecdsa, rsa, pyasn1, oauthlib, requests-oauthlib
#                               Unused auth libs. This app uses bcrypt + PyJWT.
# ast_serialize, fastuuid, librt, s5cmd, jq, typer, rich, shellingham,
# markdown-it-py, mdurl, Pygments
#                               Emergent build-agent tooling, not app code.
# aiohttp + friends, pillow, cryptography, cffi, Jinja2, MarkupSafe,
# jsonschema, watchfiles, websockets, tenacity, filelock, fsspec, regex,
# tqdm, importlib_metadata, zipp, tzdata, uritemplate, httplib2, pyparsing
#                               Transitive deps of the packages above. They
#                               come back automatically if something needs them.
